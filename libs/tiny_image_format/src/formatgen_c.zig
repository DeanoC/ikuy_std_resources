const std = @import("std");
const warn = std.log.warn;
const vfilefile = @import("vfile").VFileFile;

pub fn generateCFiles() void {
    //    var outA = try vfilefile.VFileFile.initFromPath(cwd, outputString.str(), .{ .mode = OpenMode.write_only });
    //    var out = outA.asVFile();
    //    defer out.close();
}
