name: vfile

boards: 
 - host

cpus:
 - host

builder: 
 - zig

# libraries to be linked into this peice of software
libraries: []

# non library dependencies (like host programs, asset processing etc.)
dependencies: [] 

actions:
 - fetch git git@github.com:DeanoC/ltngt.vfile.git main

zig:
  library_import: 
  library_package: executable.addPackage(std.build.Pkg{ .name = library.name, .source = .{ .path = sdkPath("vfile/package.zig") } });
  library_link:

